# 将本文件复制为 .env 或 .env.local，并替换为你的真实值
# Supabase 项目设置中可获取 URL 与 anon key：
# Supabase Dashboard > Project Settings > API

VITE_SUPABASE_URL=https://your_project_url.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# 警告：Service Role Key 拥有完全权限，绝不要暴露在前端！
# 如需使用服务端操作，请在云函数/Edge Functions/自有后端中使用。
# 当前项目 user store 支持读取该变量以“绕过RLS”，仅用于本地或受信环境测试。
# 强烈建议迁移到 Supabase Auth + RLS 或移至后端代理。
VITE_SUPABASE_SERVICE_ROLE_KEY=